{{ 'collection.css' | asset_url | stylesheet_tag }}

{%- liquid
  assign columns_desktop = section.settings.columns_desktop
  assign columns_mobile = section.settings.columns_mobile | times: 1
-%}

{% render 'divider',
  show_divider: section.settings.show_section_divider,
  divider_width: section.settings.divider_width
%}

<div
  class="section--padding color-{{ section.settings.color_scheme }}"
  style="--section-padding-top: {{ section.settings.padding_top }}px;--section-padding-bottom: {{ section.settings.padding_bottom }}px;"
>
  <div class="collection page-width">
    {%- paginate collection.products by section.settings.products_per_page -%}
      <div class="collection__content flex flex-col">
        
        <div class="collection-header-row">
          <div class="header-left">
             <p class="collection-count">
              {{ collections['all'].all_products_count }} products
            </p>
          </div>
          
          <div class="carousel-controls">
            <button id="btn-prev" class="carousel-btn" aria-label="Previous">
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"></polyline></svg>
            </button>
            
            <span id="page-counter" class="carousel-counter">Page 1 / 1</span>
            
            <button id="btn-next" class="carousel-btn" aria-label="Next">
              <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"></polyline></svg>
            </button>
          </div>
        </div>

        <div class="flex flex-col flex-grow" id="ProductGridWrapper">
          <div id="ProductGridContainer">
            
            <div id="CarouselTrack" 
                 class="carousel-track f-grid"
                 data-desktop-cols="{{ columns_desktop }}"
                 data-mobile-cols="{{ columns_mobile }}">
              
              {%- assign displayed_products = "|" -%}

              {% comment %} --- PHẦN 1: SẢN PHẨM TRONG COLLECTION --- {% endcomment %}
              {%- for product in collection.products -%}
                {%- assign displayed_products = displayed_products | append: product.handle | append: "|" -%}
                <div class="carousel-item">
                  {%- render 'card-product',
                    product: product,
                    section_id: section.id,
                    index: forloop.index
                  -%}
                </div>
              {%- endfor -%}

              {% comment %} --- PHẦN 2: SẢN PHẨM CÒN LẠI --- {% endcomment %}
              {%- if paginate.current_page == paginate.pages or paginate.pages == 0 -%}
                {%- assign other_products = collections['all'].products | sort: 'price' -%}
                {%- assign counter = 0 -%}
                {%- assign limit_extra = 24 -%} 

                {%- for product in other_products -%}
                  {%- assign check_handle = "|" | append: product.handle | append: "|" -%}
                  {%- unless displayed_products contains check_handle -%}
                    {%- if counter < limit_extra -%}
                      <div class="carousel-item">
                        {%- render 'card-product',
                          product: product,
                          section_id: section.id,
                          index: forloop.index
                        -%}
                      </div>
                      {%- assign counter = counter | plus: 1 -%}
                    {%- endif -%}
                  {%- endunless -%}
                {%- endfor -%}
              {%- endif -%}

            </div>
          </div>
        </div>
      </div>
    {%- endpaginate -%}
  </div>
</div>

<style>
  /* HEADER & CONTROLS */
  .collection-header-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
  }
  .collection-count { margin: 0; font-size: 14px; opacity: 0.8; }
  
  .carousel-controls {
    display: flex;
    align-items: center;
    gap: 10px;
  }
  .carousel-btn {
    width: 36px;
    height: 36px;
    border: 1px solid #ddd;
    border-radius: 50%;
    background: white;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.3s;
  }
  .carousel-btn:hover { background: #f0f0f0; border-color: #000; }
  .carousel-btn:disabled { opacity: 0.3; cursor: not-allowed; }
  .carousel-counter { font-size: 14px; font-weight: 500; min-width: 80px; text-align: center; }

  /* CAROUSEL TRACK LAYOUT */
  .carousel-track {
    display: flex;
    flex-wrap: nowrap; /* Không xuống dòng */
    overflow-x: auto;  /* Cho phép cuộn ngang */
    gap: 20px;
    scroll-behavior: smooth;
    -webkit-overflow-scrolling: touch;
    scroll-snap-type: x mandatory; /* Bắt dính item */
    padding-bottom: 20px; /* Chừa chỗ cho scrollbar nếu có */
    /* Ẩn thanh cuộn mặc định */
    scrollbar-width: none; /* Firefox */
    -ms-overflow-style: none;  /* IE */
  }
  .carousel-track::-webkit-scrollbar { 
    display: none; /* Chrome/Safari */
  }

  /* CAROUSEL ITEM SIZING */
  .carousel-item {
    flex: 0 0 auto; /* Không co giãn */
    scroll-snap-align: start;
    /* Tính toán chiều rộng dựa trên số cột setting */
    width: calc(100% / {{ columns_mobile }} - 10px);
  }

  @media (min-width: 768px) {
    .carousel-item {
      width: calc(100% / 3 - 15px); /* Tablet 3 cột */
    }
  }

  @media (min-width: 1024px) {
    .carousel-item {
      width: calc(100% / {{ columns_desktop }} - 20px); /* Desktop theo setting */
    }
  }
</style>

<script>
document.addEventListener("DOMContentLoaded", function() {
  const track = document.getElementById('CarouselTrack');
  const btnPrev = document.getElementById('btn-prev');
  const btnNext = document.getElementById('btn-next');
  const counterEl = document.getElementById('page-counter');

  if (!track || !btnPrev || !btnNext) return;

  function updateCounter() {
    const trackWidth = track.clientWidth;
    const scrollLeft = track.scrollLeft;
    const totalWidth = track.scrollWidth;
    
    // Tính số trang hiện tại và tổng số trang
    // Math.round để làm tròn khi scroll chưa tới hẳn
    const currentPage = Math.round(scrollLeft / trackWidth) + 1;
    const totalPages = Math.round(totalWidth / trackWidth);
    
    // Cập nhật text (VD: Page 1 / 4)
    // Nếu totalPages = 1 (ít sản phẩm) thì ghi 1/1
    const maxPage = totalPages < 1 ? 1 : totalPages;
    counterEl.innerText = `Page ${currentPage} / ${maxPage}`;

    // Disable nút nếu ở đầu/cuối
    btnPrev.disabled = scrollLeft <= 10; // Có sai số nhỏ
    btnNext.disabled = scrollLeft >= (totalWidth - trackWidth - 10);
  }

  // Sự kiện bấm nút
  btnPrev.addEventListener('click', () => {
    track.scrollBy({ left: -track.clientWidth, behavior: 'smooth' });
  });

  btnNext.addEventListener('click', () => {
    track.scrollBy({ left: track.clientWidth, behavior: 'smooth' });
  });

  // Sự kiện khi người dùng tự vuốt/scroll
  track.addEventListener('scroll', () => {
    // Dùng requestAnimationFrame hoặc debounce để đỡ lag (ở đây làm đơn giản)
    updateCounter();
  });

  // Chạy lần đầu
  updateCounter();
  
  // Cập nhật lại khi resize cửa sổ
  window.addEventListener('resize', updateCounter);
});
</script>

{% schema %}
{
  "name": "Carousel Mix Price",
  "class": "collection-section",
  "settings": [
    {
      "type": "paragraph",
      "content": "Dạng trượt ngang (Carousel) với nút điều hướng."
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "range",
      "id": "products_per_page",
      "min": 8,
      "max": 50,
      "step": 1,
      "default": 16,
      "label": "Products limit"
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "min": 2,
      "max": 6,
      "step": 1,
      "default": 4,
      "label": "Columns on Desktop"
    },
    {
      "type": "select",
      "id": "columns_mobile",
      "default": "2",
      "label": "Columns on Mobile",
      "options": [
        { "value": "1", "label": "1 Column" },
        { "value": "2", "label": "2 Columns" }
      ]
    },
    {
      "type": "checkbox",
      "id": "show_section_divider",
      "default": false,
      "label": "Show divider"
    },
    {
      "type": "select",
      "id": "divider_width",
      "options": [
        { "value": "fixed", "label": "Fixed" },
        { "value": "full", "label": "Full" }
      ],
      "default": "fixed",
      "label": "Divider width"
    },
    {
        "type": "header",
        "content": "Padding"
    },
    {
        "type": "range",
        "id": "padding_top",
        "min": 0,
        "max": 100,
        "step": 2,
        "unit": "px",
        "label": "Padding Top",
        "default": 50
    },
    {
        "type": "range",
        "id": "padding_bottom",
        "min": 0,
        "max": 100,
        "step": 2,
        "unit": "px",
        "label": "Padding Bottom",
        "default": 50
    }
  ]
}
{% endschema %}